#include <stdio.h>
#include <stdlib.h>
/**
 * main -  print the alphabet in lowercase, and then in uppercase
 * Return: Always 0 (success)
 */
int main(void)
{
	int ne;

	for (ne = 'a'; ne <= 'z'ch++)
	{
		putchar(ne);
	}
	for (ne = 'A'; ne <= 'Z'; ch++)
	{
		putchar(ne);
	}
	putchar('\n');
	return (0);
}
